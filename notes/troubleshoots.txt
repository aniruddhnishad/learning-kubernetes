when worker node not ready mostly i encounter the dns error
fix this using below commands
runcmd:
  - mkdir -p /run/systemd/resolve
  - ln -sf /etc/resolv.conf /run/systemd/resolve/resolv.conf

make control panel taint #
# mean it skip any pod schedule on control panel. 
kubectl taint nodes deluxhostf3.incserver.de node-role.kubernetes.io/control-plane:NoSchedule

###to make control panle host pods as well run 

kubectl taint nodes deluxhostf3.incserver.de node-role.kubernetes.io/control-plane:NoSchedule-